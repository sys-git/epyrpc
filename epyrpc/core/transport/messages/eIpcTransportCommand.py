
from epyrpc.utils.Enum import Enum

class eIpcTransportCommand(Enum):
    r"""
    @requirements undefined for now.
    """
    PING = 0
    DISCONNECT = 1
    DISCONNECTED = 2
eIpcTransportCommand()
